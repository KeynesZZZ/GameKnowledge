---
title: 【架构决策】事件系统实现对比
tags: [C#, Unity, 架构, 事件系统, 观察者模式, 架构决策, C#事件, UnityEvent]
category: 架构设计/架构决策
created: 2024-01-23 11:00
updated: 2026-03-04 22:02
description: Unity中多种事件系统实现方式的对比分析，包含C#事件、UnityEvent、事件总线、ScriptableObject事件
unity_version: 2021.3+
---

# 【架构决策】事件系统实现对比

> 核心问题：Unity中有哪些事件系统实现方式？各有什么优缺点？

## 概述

事件系统是解耦组件通信的核心机制，本篇对比Unity中常见的实现方式。

## 实现方式

### 1. C# 原生事件

```csharp
public class EventProvider
{
    public event Action<int> OnScoreChanged;

    public void UpdateScore(int score)
    {
        OnScoreChanged?.Invoke(score);
    }
}

public class EventConsumer
{
    public EventConsumer(EventProvider provider)
    {
        provider.OnScoreChanged += HandleScoreChanged;
    }

    private void HandleScoreChanged(int score)
    {
        Debug.Log($"Score: {score}");
    }
}
```

**优点**: 类型安全、零分配
**缺点**: 需要手动取消订阅、强引用

### 2. UnityEvent

```csharp
public class UnityEventExample : MonoBehaviour
{
    [SerializeField] private UnityEvent<int> onScoreChanged;

    public void TriggerScore(int score)
    {
        onScoreChanged.Invoke(score);
    }
}
```

**优点**: Inspector可配置、支持持久化监听
**缺点**: 每次Invoke有分配、性能较低

### 3. ScriptableObject事件

```csharp
[CreateAssetMenu(fileName = "GameEvent", menuName = "Events/GameEvent")]
public class GameEvent : ScriptableObject
{
    private readonly List<GameEventListener> listeners = new();

    public void Raise()
    {
        for (int i = listeners.Count - 1; i >= 0; i--)
        {
            listeners[i].OnEventRaised();
        }
    }

    public void RegisterListener(GameEventListener listener)
    {
        listeners.Add(listener);
    }

    public void UnregisterListener(GameEventListener listener)
    {
        listeners.Remove(listener);
    }
}
```

**优点**: 架构解耦、可跨场景使用
**缺点**: 需要额外的Listener组件

### 4. EventBus（全局事件总线）

```csharp
public static class EventBus
{
    private static readonly Dictionary<Type, List<Delegate>> events = new();

    public static void Subscribe<T>(Action<T> callback) where T : struct
    {
        var type = typeof(T);
        if (!events.ContainsKey(type))
        {
            events[type] = new List<Delegate>();
        }
        events[type].Add(callback);
    }

    public static void Publish<T>(T eventData) where T : struct
    {
        var type = typeof(T);
        if (events.TryGetValue(type, out var callbacks))
        {
            foreach (var callback in callbacks)
            {
                ((Action<T>)callback).Invoke(eventData);
            }
        }
    }
}

// 使用结构体避免分配
public struct ScoreChangedEvent
{
    public int Score;
}
```

**优点**: 完全解耦、类型安全
**缺点**: 需要管理订阅生命周期

## 性能对比

### 基准测试数据

> 测试环境: Unity 2021.3, Windows 11, .NET Standard 2.1
> 测试方法: 1000次Invoke调用，测量平均耗时

| 实现方式 | Invoke耗时 | 订阅/取消 | 内存分配(GC) | 类型安全 |
|----------|------------|----------|--------------|----------|
| **C# Event** | ~0.001ms | O(1) | 0B | ✅ 编译时 |
| **UnityEvent** | ~0.05ms | O(n) | 40B/Invoke | ✅ 运行时 |
| **SO Event** | ~0.02ms | O(n) | 0B | ❌ 无 |
| **EventBus** | ~0.01ms | O(1) | 0B | ✅ 泛型 |

### 性能分析

```
┌─────────────────────────────────────────────────────────────┐
│                    性能排序（从快到慢）                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. C# 原生事件                                             │
│     └─ 直接委托调用，无额外开销                             │
│                                                             │
│  2. EventBus（泛型）                                        │
│     └─ 字典查找 + 委托调用，开销极小                        │
│                                                             │
│  3. ScriptableObject Event                                  │
│     └─ 列表遍历 + 反射调用（可选）                          │
│                                                             │
│  4. UnityEvent                                              │
│     └─ 每次Invoke产生40B GC分配                            │
│     └─ 内部使用PersistentCall数组                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 高频事件场景测试

| 场景 | 事件频率 | 推荐方案 | GC压力 |
|------|----------|----------|--------|
| 每帧位置同步 | 60次/秒 | C# Event | 0B |
| UI按钮点击 | <1次/秒 | UnityEvent | 可忽略 |
| 游戏状态变化 | 10次/秒 | EventBus | 0B |
| 配置加载 | 1次/场景 | SO Event | 0B |

### 内存泄漏风险

| 实现方式 | 忘记取消订阅 | 风险等级 |
|----------|--------------|----------|
| C# Event | 导致对象无法释放 | 🔴 高 |
| UnityEvent | 自动弱引用 | 🟢 低 |
| SO Event | 需手动管理 | 🟡 中 |
| EventBus | 需手动管理 | 🟡 中 |

## 最佳实践

### DO ✅

- 使用结构体作为事件数据避免GC
- 在OnDisable/OnDestroy时取消订阅
- 优先使用类型安全的事件系统

### DON'T ❌

- 不要在事件处理中抛出异常
- 不要创建循环事件依赖
- 不要忘记取消订阅导致内存泄漏

## 选择指南

| 场景 | 推荐方案 |
|------|----------|
| Inspector配置 | UnityEvent |
| 跨系统通信 | EventBus |
| 配置驱动 | ScriptableObject Event |
| 高频事件 | C# 原生事件 |

## 相关链接

> 本文档是事件系统系列的一部分，建议配合阅读：

- [[代码片段-事件总线]] - 事件总线的完整实现代码
- [[设计原理-事件驱动本质]] - 事件驱动的底层原理
- [事件系统与观察者模式](../../学习/01-脚本与架构/事件系统与观察者模式.md) - 学习教程
